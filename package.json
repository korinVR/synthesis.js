{
  "name": "synthesis.js",
  "version": "0.0.0",
  "description": "",
  "main": "src/main.js",
  "author": "Katsuomi Kobayashi",
  "license": "MIT",
  "scripts": {
    "build": "browserify src/main.js | uglifyjs > build/synthesis.js",
    "build:demo": "browserify demo/main.js | uglifyjs > build/demo.js",
    "start": "browser-sync start --server --no-ghost-mode --files \"index.html, build/**\" --reload-delay 200",
    "watch": "parallelshell \"watchify src/main.js -o build/synthesis.js --debug --verbose --delay 50\" \"watchify demo/main.js -o build/demo.js --debug --verbose --delay 50\" \"npm start\"",
    "test": "mocha --compilers js:babel/register"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "dependencies": {
    "mml2smf": "*"
  },
  "devDependencies": {
    "browserify": "*",
    "watchify": "*", 
    "babelify": "*",
    "uglifyjs": "*",
    "browser-sync": "*",
    "parallelshell": "*",
    "mocha": "*"
  }
}
